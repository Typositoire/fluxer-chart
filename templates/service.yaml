apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxer-chart.fullname" . }}
  labels:
    {{- include "fluxer-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluxer-chart.selectorLabels" . | nindent 4 }}
